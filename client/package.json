{
  "name": "@easy-asphalt/client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config ../vite.config.ts",
    "build": "vite build --config ../vite.config.ts",
    "check": "tsc --noEmit -p ../tsconfig.json",
    "test": "vitest run --config ../vitest.config.ts client/src"
  }
}
